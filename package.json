{
  "name": "metabase-poc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && yarn install && yarn build && cd ../server && yarn install && npx tsc",
    "start": "node server/dist/index.js"
  }
}
